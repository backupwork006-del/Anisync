# Project Instructions

- **Build APK after every change**: Always compile the applet and build the APK (using `compile_applet`) after making any code changes.
- **Maintain APK in `apk/` directory**: Keep `apk/app-debug.apk` committed in the repository so the user can download and install it directly from GitHub after pushing changes.
